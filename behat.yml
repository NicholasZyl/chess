default:
  suites:
    game_rules:
      contexts:
        - ChessboardContext:
          - '@chess_piece_factory'
      services:
        chess_piece_factory: Helper\PieceFactory
  formatters:
    pretty: true