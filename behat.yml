default:
  suites:
    domain:
      contexts:
        - DomainContext:
          - '@chess_piece_factory'
      services:
        chess_piece_factory: Helper\PieceFactory
    application:
      contexts:
        - AppContext
      filters:
        tags: '@application'
  formatters:
    pretty: true